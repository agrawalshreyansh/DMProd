# Worker service (app.workers.run_worker / Dockerfile.worker).
#
# Every third-party import reachable from run_worker: the RQ loop, the reel
# pipeline (download -> audio -> Groq transcription -> visual analysis ->
# task generation) and the push jobs (Notion, Google Calendar).
# Subset of requirements.txt minus the web-server (fastapi/uvicorn) and auth
# (bcrypt/pyjwt) deps, which no worker code path imports.

# db
beanie==1.26.0
motor==3.7.1
pymongo==4.17.0
certifi==2026.7.22

# config / models
pydantic-settings==2.5.2
email-validator==2.2.0

# queue
rq==2.11.0
redis==8.1.0

# pipeline + integrations
httpx==0.28.1
cryptography==43.0.1
notion-client==3.1.0
instagrapi==2.18.18
yt-dlp==2026.8.19
google-genai==2.19.0
Pillow==12.3.0
ImageHash==4.3.2
