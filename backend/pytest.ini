[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
markers =
    slow: hits a real network URL (yt-dlp); excluded by default, run with -m slow
addopts = -m "not slow"
