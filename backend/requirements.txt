# API service (app.main / Dockerfile).
#
# app/api/webhooks.py and app/api/integrations.py import app/workers/* at
# module load time, so the API process pulls in the full worker dependency
# tree too. This file is therefore requirements-worker.txt plus the
# web-server and auth deps.

# web server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# auth (app/services/auth.py) — API only
bcrypt==4.2.0
pyjwt==2.9.0

# db
beanie==1.26.0
motor==3.7.1
pymongo==4.17.0
certifi==2026.7.22

# config / models
pydantic-settings==2.5.2
email-validator==2.2.0

# queue
rq==2.11.0
redis==8.1.0

# integrations / worker pipeline (imported eagerly via webhooks + integrations)
httpx==0.28.1
cryptography==43.0.1
notion-client==3.1.0
instagrapi==2.18.18
yt-dlp==2026.8.19
google-genai==2.19.0
Pillow==12.3.0
ImageHash==4.3.2
